services:
  mysql:
    image: mysql:8.4
    container_name: resource-monitor-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: resource_monitor
      MYSQL_USER: monitor_user
      MYSQL_PASSWORD: monitor_password
      MYSQL_ROOT_PASSWORD: root_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: